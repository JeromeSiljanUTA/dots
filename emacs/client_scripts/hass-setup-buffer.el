;;; hass-setup-buffer.el --- Setup for hass dashboard.

;;; Commentary:
;; Enable writeroom to improve the zen of the buffer.

;;; Code:
(defun hass-setup-buffer()
  "Set up hass buffer."
  (writeroom-mode))

;;; hass-setup-buffer.el ends here
